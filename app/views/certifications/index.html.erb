<h1>Listing certifications</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th>Certification</th>
      <th>Company</th>
      <th>Category</th>
      <th>Description</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @certifications.each do |certification| %>
      <tr>
        <td><img src="<%= certification.logo_url %>" alt="microsoft logo" hieght="40" width="40"></td>
        <td><%= certification.cert_name %></td>
        <td><%= certification.cert_company %></td>
        <td><%= certification.category %></td>
        <td><%= certification.description %></td>
        <td><%= link_to 'Show', certification %></td>
        <td><%= link_to 'Edit', edit_certification_path(certification) %></td>
        <td><%= link_to 'Destroy', certification, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Certification', new_certification_path %>
